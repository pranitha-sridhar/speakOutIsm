<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appitup">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppItUp">
        <activity android:name=".activities.SplashAtivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ConversationActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppItUp.NoActionBar" />
        <activity android:name=".activities.Register_Complaint" />
        <activity android:name=".activities.NotificationsActivity" />
        <activity
            android:name=".activities.SignIn"
            android:theme="@style/Theme.AppItUp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ResetPassword" />
        <activity
            android:name=".activities.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppItUp.NoActionBar" />
        <activity android:name=".activities.SignUpActivity" />
    </application>

</manifest>